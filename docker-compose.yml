version: '3.7'
# TDOO: add the command to serve the slides in the browser
services:
  presentation:
    build: .
    # user: 1001:1001 
    volumes:
      - .:/reveal.js/content:Z
    ports:
      - 8000:8000
    # command: " --standalone -t revealjs -o /data/presentation.html -s /data/presentation.md -V theme=$solarized -V revealjs-url=https://revealjs.com"
    
volumes:
  presentation:
    driver: local
  slides:
    driver: local